{
  "name": "taco-apt",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static site - no build needed'",
    "start": "npx serve ."
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}
